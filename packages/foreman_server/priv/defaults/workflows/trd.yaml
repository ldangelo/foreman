name: trd
description: Create a TRD from a PRD and implement it via the ensemble chain.
phases:
  - name: create-trd
    command: "/skill:ensemble-full-create-trd --foreman"
  - name: implement-trd
    command: "/skill:ensemble-full-implement-trd --foreman"
