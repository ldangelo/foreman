name: assess
description: Analyze impact, constraints, and risks before implementation begins.
phases:
  - name: impact-analysis
    prompt: assess.md
    provider: pi
    models:
      default: minimax/MiniMax-M2.7
    maxTurns: 45
    artifact: "{task.projectReportsDir}/ASSESS_REPORT.md"
    mail:
      onStart: true
      onComplete: true
