# Bug fix workflow: explicit exploration → fix prompt → QA validation → developer remediation on failure → finalize → merge
#
# Lean bug-fix pipeline:
#   - explorer prompt — read-only developer handoff, no nested delegation
#   - fix prompt — workflow-scoped issue fixing prompt
#   - qa phase — validates the fix and writes QA_REPORT.md
#   - developer prompt — retry-only target for QA/review/finalize findings
#
# Auto-selected when a task has type "bug" (TRD-006 type-based dispatch).
# Use `workflow:` label to override, e.g. `workflow:smoke` for smoke workflow.

name: bug
task_type: bug
setup:
  - command: npm install --prefer-offline --no-audit
    description: Install Node.js dependencies
    failFatal: true
setupCache:
  key: package-lock.json
  path: node_modules

phases:
  # ── Phase 1: explorer ───────────────────────────────────────────────────
  # Explicit read-only exploration avoids hidden discovery inside the editing
  # phase and produces a durable handoff artifact.
  - name: explorer
    prompt: explorer.md
    tools:
      allowed: [GraphifyQuery, GraphifyExplain, Glob, Read, Write]
    models:
      default: MiniMax
      P0: MiniMax-highspeed
    maxTurns: 500
    artifact: "{task.projectReportsDir}/EXPLORER_REPORT.md"
    skipIfArtifact: EXPLORER_REPORT.md
    mail:
      onStart: true
      onComplete: true
      forwardArtifactTo: fix

  # ── Phase 2: fix ────────────────────────────────────────────────────────
  # Uses the workflow-scoped fix-issue prompt so the artifact contract is
  # explicit and isolated from other workflows. Nested delegation tools are
  # intentionally omitted; exploration is the prior explicit phase.
  - name: fix
    checkpointPr: true
    prompt: fix-issue.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, TodoWrite, WebFetch, WebSearch, Write]
    models:
      default: MiniMax
      P0: MiniMax
      P4: MiniMax
    maxTurns: 500
    artifact: "{task.projectReportsDir}/DEVELOPER_REPORT.md"
    mail:
      onStart: true
      onComplete: true

  # ── Retry-only: developer remediation ──────────────────────────────────
  # Retry target for QA/finalize/PR-review findings. Skipped during normal
  # sequential execution; activated only when a later phase fails and uses
  # retryWith: developer.
  - name: developer
    checkpointPr: true
    retryOnly: true
    prompt: developer.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, TodoWrite, WebFetch, WebSearch, Write]
    models:
      default: MiniMax
      P0: MiniMax
      P4: MiniMax
    maxTurns: 500
    artifact: "{task.projectReportsDir}/DEVELOPER_REPORT.md"
    mail:
      onStart: true
      onComplete: true
    files:
      reserve: true
      leaseSecs: 600

  - name: cicd-developer
    checkpointPr: true
    retryOnly: true
    prompt: developer.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, TodoWrite, WebFetch, WebSearch, Write]
    models:
      default: MiniMax
      P0: MiniMax
      P4: MiniMax
    maxTurns: 500
    artifact: "{task.projectReportsDir}/DEVELOPER_REPORT.md"
    mail:
      onStart: true
      onComplete: true
    files:
      reserve: true
      leaseSecs: 600

  - name: cr-developer
    checkpointPr: true
    retryOnly: true
    prompt: developer.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, TodoWrite, WebFetch, WebSearch, Write]
    models:
      default: MiniMax
      P0: MiniMax
      P4: MiniMax
    maxTurns: 500
    artifact: "{task.projectReportsDir}/DEVELOPER_REPORT.md"
    mail:
      onStart: true
      onComplete: true
    files:
      reserve: true
      leaseSecs: 600

  - name: merge-resolver
    checkpointPr: true
    retryOnly: true
    prompt: developer.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, TodoWrite, WebFetch, WebSearch, Write]
    models:
      default: MiniMax
      P0: MiniMax
      P4: MiniMax
    maxTurns: 500
    artifact: "{task.projectReportsDir}/DEVELOPER_REPORT.md"
    mail:
      onStart: true
      onComplete: true
    files:
      reserve: true
      leaseSecs: 600

  # ── Phase 3: documentation ──────────────────────────────────────────────
  - name: documentation
    checkpointPr: true
    prompt: documentation.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, Write]
    models:
      default: MiniMax
      P0: MiniMax-highspeed
    maxTurns: 500
    artifact: "{task.projectReportsDir}/DOCUMENTATION_REPORT.md"
    mail:
      onStart: true
      onComplete: true
      forwardArtifactTo: foreman

  - name: qa
    prompt: qa.md
    tools:
      allowed: [Bash, Edit, Glob, Grep, Read, TodoWrite, Write]
    models:
      default: MiniMax
      P0: MiniMax
      P4: MiniMax
    maxTurns: 500
    artifact: "{task.projectReportsDir}/QA_REPORT.md"
    verdict: true
    retryWith: developer
    retryWithByReason:
      "ci_failed:": cicd-developer
      "pr_review_failed_checks:": cicd-developer
      "coderabbit_": cr-developer
      "merge_conflict:": merge-resolver
    retryOnFail: 2
    mail:
      onStart: true
      onComplete: true
      onFail: developer

  # ── Phase 4: finalize (prompt phase) ───────────────────────────────────
  # Standard finalize with human-readable summary for auto-merge context.
  # artifact: "{task.projectReportsDir}/FINALIZE_VALIDATION.md" is what the pipeline reads for the validation
  # contract check (Target Integration / Test Validation sections + Verdict).
  # FINALIZE_REPORT.md is also written by the agent as a human-readable summary.
  - name: finalize
    builtin: true
    artifact: "{task.projectReportsDir}/FINALIZE_VALIDATION.md"
    maxTurns: 500
    verdict: true
    retryWith: developer
    retryOnFail: 1
    mail:
      onStart: true
      onComplete: true
      onFail: developer
  - name: create-pr
    builtin: true
    artifact: "{task.projectReportsDir}/PR_METADATA.json"
    mail:
      onStart: true
      onComplete: true

  - name: pr-wait
    builtin: true
    artifact: "{task.projectReportsDir}/PR_WAIT_REPORT.md"
    timeoutSecs: 1200
    retryWith: developer
    retryWithByReason:
      "ci_failed:": cicd-developer
      "pr_review_failed_checks:": cicd-developer
      "coderabbit_": cr-developer
      "merge_conflict:": merge-resolver
    retryOnFail: 2
    mail:
      onStart: true
      onComplete: true
      onFail: developer


  - name: merge
    builtin: true
    artifact: "{task.projectReportsDir}/MERGE_REPORT.md"
    retryWith: developer
    retryWithByReason:
      "ci_failed:": cicd-developer
      "pr_review_failed_checks:": cicd-developer
      "coderabbit_": cr-developer
      "merge_conflict:": merge-resolver
    retryOnFail: 2
    mail:
      onStart: true
      onComplete: true
      onFail: developer
